# Production stage - assumes dist folder is pre-built
FROM nginx:alpine
COPY dist/irrigation-frontend /usr/share/nginx/html
COPY nginx.conf /etc/nginx/nginx.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
